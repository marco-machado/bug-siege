---
phase: 01-cosmic-foundation
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/scenes/BootScene.js, src/scenes/GameScene.js]
autonomous: true
requirements: [THEME-02]
must_haves:
  truths:
    - "Game background is a procedurally generated nebula, not a static image"
    - "Nebula colors are derived from the THEME config"
  artifacts:
    - path: "src/scenes/BootScene.js"
      provides: "Nebula texture generation logic"
    - path: "src/scenes/GameScene.js"
      provides: "Application of 'nebula' texture to background"
  key_links:
    - from: "src/scenes/BootScene.js"
      to: "Phaser Texture Manager"
      via: "this.textures.create / addCanvas"
    - from: "src/scenes/GameScene.js"
      to: "Phaser Texture Manager"
      via: "this.add.image(..., 'nebula')"
---

<objective>
Implement a procedurally generated nebula background that replaces the static background image, creating a gaseous, cosmic atmosphere.

Purpose: Transitions the game from a utilitarian look to the "Cosmic Nebula" aesthetic.
Output: A dynamic 'nebula' texture created at boot and used in the main game scene.
</objective>

<execution_context>
@/Users/machado/Projects/bug-siege/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/bug-siege/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cosmic-foundation/01-CONTEXT.md
@src/config/GameConfig.js
@src/scenes/BootScene.js
@src/scenes/GameScene.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Procedural Nebula Generation in BootScene</name>
  <files>src/scenes/BootScene.js</files>
  <action>
    Implement a method in `BootScene` to generate a procedural nebula texture of 1920x1080.
    
    Implementation details per D-02:
    1. Create a canvas of 1920x1080.
    2. Fill the background with `THEME.background`.
    3. Draw 10-15 large, overlapping radial gradients.
       - Position: Random across the canvas.
       - Radius: Large (e.g., 400px to 800px).
       - Color: Randomly selected from `THEME.nebula` array.
       - Opacity: Low (0.1 to 0.3) to create a soft, gaseous accumulation effect.
    4. Use `this.textures.addCanvas('nebula', canvas)` to add the result to the global texture cache.
    
    Call this generation logic during the `create()` method of `BootScene`, after assets are loaded but before transitioning to the next scene.
  </action>
  <verify>
    <automated>grep "this.textures.addCanvas('nebula'" src/scenes/BootScene.js</automated>
  </verify>
  <done>A 'nebula' texture is procedurally generated and cached in the texture manager during boot.</done>
</task>

<task type="auto">
  <name>Task 2: Update GameScene to Use Nebula Background</name>
  <files>src/scenes/GameScene.js</files>
  <action>
    In `src/scenes/GameScene.js`, locate the line where the background image is added (currently `this.add.image(..., 'background')`).
    Change the texture key from `'background'` to `'nebula'`.
  </action>
  <verify>
    <automated>grep "this.add.image(GAME.canvasWidth / 2, GAME.canvasHeight / 2, 'nebula')" src/scenes/GameScene.js</automated>
  </verify>
  <done>GameScene now displays the procedural nebula background instead of the static image.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
None. Client-side rendering.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | Canvas Memory | accept | 1920x1080 canvas is well within memory limits of modern browsers. |
</threat_model>

<verification>
1. Start the game and verify the background is no longer a static image but a soft, purple-toned nebula.
2. Verify that the background colors match the "Void Ethereal" palette.
3. Run `npm run build` to ensure no syntax errors.
</verification>

<success_criteria>
- `BootScene` successfully generates a 'nebula' canvas texture.
- `GameScene` renders the 'nebula' texture as the main background.
- Visuals align with the "Void Ethereal" theme.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cosmic-foundation/01-02-SUMMARY.md`
</output>
