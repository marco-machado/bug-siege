---
phase: 01-cosmic-foundation
plan: 03
type: execute
wave: 1
depends_on: [01, 02]
files_modified:
  - src/config/GameConfig.js
  - src/main.js
autonomous: true
gap_closure: true
requirements: [THEME-01]

must_haves:
  truths:
    - "THEME.background matches UI-SPEC dominant color #0a0a12"
    - "THEME.nebula[0] matches UI-SPEC secondary color #2d1b4e"
    - "THEME.accent matches UI-SPEC accent color #eef2ff"
    - "Phaser config backgroundColor matches UI-SPEC dominant color #0a0a12"
  artifacts:
    - path: "src/config/GameConfig.js"
      provides: "Corrected THEME palette matching UI-SPEC contract"
      contains: "#0a0a12"
    - path: "src/main.js"
      provides: "Corrected Phaser fallback background color"
      contains: "#0a0a12"
  key_links:
    - from: "src/config/GameConfig.js"
      to: "src/scenes/BootScene.js"
      via: "THEME import for nebula generation"
      pattern: "import.*THEME.*GameConfig"
---

<objective>
Fix THEME config color values to match the UI-SPEC design contract. The Phase 1 implementation introduced correct color roles but with values that diverge from the spec. This plan corrects four hex values across two files.

Purpose: The UI-SPEC defines the canonical color contract. Phase 1 implemented close-but-wrong values. Downstream phases (especially Phase 7: Cohesive Theme) will propagate these colors across the entire codebase, so the foundation must be exact.

Output: Corrected THEME config in GameConfig.js and corrected Phaser fallback background in main.js.
</objective>

<execution_context>
@/Users/machado/Projects/bug-siege/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/bug-siege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cosmic-foundation/01-UI-SPEC.md
@.planning/phases/01-cosmic-foundation/01-UI-REVIEW.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct THEME palette and Phaser fallback to match UI-SPEC</name>
  <files>src/config/GameConfig.js, src/main.js</files>
  <read_first>
    src/config/GameConfig.js
    src/main.js
    .planning/phases/01-cosmic-foundation/01-UI-SPEC.md
  </read_first>
  <action>
Update four hex color values to match the UI-SPEC design contract exactly.

In src/config/GameConfig.js, update the THEME object (lines 132-136):
- Change `background` from `'#05050a'` to `'#0a0a12'`
- Change `nebula` array first element from `'#2a1b3d'` to `'#2d1b4e'`
- Change `accent` from `'#e0e0ff'` to `'#eef2ff'`
- DO NOT change `nebula[1]` (`'#4b2c62'`) or `nebula[2]` (`'#6a4c93'`) -- these are intermediate nebula cloud colors used by BootScene's procedural generation and are intentionally not in the UI-SPEC

The corrected THEME block should read:
```js
export const THEME = Object.freeze({
  background: '#0a0a12',
  nebula: Object.freeze(['#2d1b4e', '#4b2c62', '#6a4c93']),
  accent: '#eef2ff',
});
```

In src/main.js, update the Phaser config (line 14):
- Change `backgroundColor` from `'#1a1a2e'` to `'#0a0a12'`

This aligns the Phaser fallback with the spec's dominant color, so the brief flash before BootScene runs matches the theme.
  </action>
  <verify>
    <automated>grep "'#0a0a12'" src/config/GameConfig.js && grep "'#2d1b4e'" src/config/GameConfig.js && grep "'#eef2ff'" src/config/GameConfig.js && grep "'#0a0a12'" src/main.js && npm run build</automated>
  </verify>
  <acceptance_criteria>
- grep "'#0a0a12'" src/config/GameConfig.js returns the background line
- grep "'#2d1b4e'" src/config/GameConfig.js returns the nebula array line
- grep "'#eef2ff'" src/config/GameConfig.js returns the accent line
- grep "'#0a0a12'" src/main.js returns the backgroundColor line
- grep "'#05050a'" src/config/GameConfig.js returns NO matches (old value removed)
- grep "'#e0e0ff'" src/config/GameConfig.js returns NO matches (old value removed)
- grep "'#2a1b3d'" src/config/GameConfig.js returns NO matches (old value removed)
- grep "'#1a1a2e'" src/main.js returns NO matches (old value removed)
- npm run build completes with exit code 0
  </acceptance_criteria>
  <done>All four THEME/config color values match the UI-SPEC contract exactly. No old values remain. Build succeeds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries affected -- this is a config value correction in a client-side game with no network communication or user input processing.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | T (Tampering) | GameConfig.js | accept | Client-side game config; no security implications from color values |
</threat_model>

<verification>
1. All four grep checks pass (correct values present)
2. All four negative grep checks pass (old values absent)
3. `npm run build` succeeds with exit code 0
</verification>

<success_criteria>
THEME.background is '#0a0a12', THEME.nebula[0] is '#2d1b4e', THEME.accent is '#eef2ff', and Phaser config backgroundColor is '#0a0a12'. Build passes. The nebula procedural generation in BootScene will automatically pick up the corrected colors on next run.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cosmic-foundation/01-03-SUMMARY.md`
</output>
