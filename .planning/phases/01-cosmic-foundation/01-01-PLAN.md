---
phase: 01-cosmic-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/config/GameConfig.js, src/entities/Bug.js]
autonomous: true
requirements: [THEME-01, ANIM-05]
must_haves:
  truths:
    - "A frozen THEME config object exists with the 'Void Ethereal' palette"
    - "Bugs are cleaned of all active tweens when despawned"
  artifacts:
    - path: "src/config/GameConfig.js"
      provides: "THEME color constants"
    - path: "src/entities/Bug.js"
      provides: "Tween cleanup in despawn()"
  key_links:
    - from: "src/entities/Bug.js"
      to: "Phaser Tween Manager"
      via: "this.scene.tweens.killTweensOf(this)"
---

<objective>
Establish the cosmic color palette and implement a safety net for pooled entity tweens to prevent visual corruption during reuse.

Purpose: Provides the color foundation for all subsequent polish and ensures technical stability for bug animations.
Output: Frozen THEME configuration and stable Bug despawn logic.
</objective>

<execution_context>
@/Users/machado/Projects/bug-siege/.opencode/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/bug-siege/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-cosmic-foundation/01-CONTEXT.md
@src/config/GameConfig.js
@src/entities/Bug.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define Void Ethereal Theme Palette</name>
  <files>src/config/GameConfig.js</files>
  <action>
    Add a new frozen `THEME` constant to `src/config/GameConfig.js`. 
    The palette should follow the "Void Ethereal" decision (D-01):
    - `background`: Near-black (`#05050a`)
    - `nebula`: Array of soft violets (`#2a1b3d`, `#4b2c62`, `#6a4c93`)
    - `accent`: Ghostly white (`#e0e0ff`)
    
    Ensure the object is frozen using `Object.freeze()` at both top and nested levels.
  </action>
  <verify>
    <automated>grep "export const THEME = Object.freeze" src/config/GameConfig.js</automated>
  </verify>
  <done>THEME config exists in GameConfig.js and contains the specified colors.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Tween Safety Net in Bug Despawn</name>
  <files>src/entities/Bug.js</files>
  <action>
    Update the `despawn()` method in `src/entities/Bug.js` to kill all active tweens targeting the bug instance.
    Add `this.scene.tweens.killTweensOf(this);` as the first line of the `despawn()` method.
    This prevents orphaned tweens from continuing to run on a pooled entity after it has been deactivated, per D-03.
  </action>
  <verify>
    <automated>grep "killTweensOf(this)" src/entities/Bug.js</automated>
  </verify>
  <done>Bug.despawn() now clears all active tweens, preventing corruption upon reuse.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
None. Internal game logic modifications.

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Denial of Service | Tween Manager | accept | Phaser's Tween Manager handles large numbers of tweens efficiently; no risk of memory leak from small number of per-entity tweens. |
</threat_model>

<verification>
1. Verify `GameConfig.js` exports `THEME` with the correct colors.
2. Verify `Bug.js` calls `killTweensOf` in `despawn()`.
3. Run `npm run build` to ensure no syntax errors were introduced.
</verification>

<success_criteria>
- `THEME` object is present, frozen, and uses the Void Ethereal palette.
- `Bug.despawn()` kills active tweens.
- Build completes without warnings.
</success_criteria>

<output>
After completion, create `.planning/phases/01-cosmic-foundation/01-01-SUMMARY.md`
</output>
