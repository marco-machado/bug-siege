---
phase: 06-cohesive-theme
plan: 06
plan_id: 06-06
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/systems/BuildSystem.js
autonomous: true
requirements: [THEME-03]
tags: [theme, system, menu, refactor]

must_haves:
  truths:
    - "BuildSystem imports THEME from GameConfig.js"
    - "Build menu and upgrade menu backgrounds/borders use ui.surface / ui.surfaceBorder"
    - "Can-afford / can-upgrade / can-repair labels flip between textPrimary and textDisabled via THEME.ui.*"
    - "All pointer hover handlers use accentPrimary on hover, restore from stored item.color on hover-out"
    - "flashDenied() flashes ui.danger.hex, restores from saved origColor string"
    - "Grid hover highlight (empty vs occupied) uses accentPrimary / surfaceBorder"
    - "npm run build exits 0 with no warnings"
  artifacts:
    - path: "src/systems/BuildSystem.js"
      provides: "Build/upgrade/sell menus consuming THEME.ui palette"
      contains: "THEME.ui.surface.num"
  key_links:
    - from: "src/systems/BuildSystem.js"
      to: "src/config/GameConfig.js"
      via: "import { TURRETS, GAME, THEME } from '../config/GameConfig.js'"
      pattern: "import.*THEME.*from.*GameConfig"
---

<objective>
Migrate BuildSystem.js — the largest consumer (~24 literal replacements across 19 source lines) — to `THEME.ui.*`. This file is the core of the build/upgrade/repair/sell menu system, so it composes every migration shape: Pattern B (text), Pattern C (hover pair), Pattern D (Rectangle + setStrokeStyle), Pattern G (conditional hover highlight), Pattern H (flashDenied), Pattern I (ternary text color for can-afford states).

Purpose: Replace scattered `#00ff88`, `#ffdd00`, `#ffffff`, `#555555`, `#444444`, `#88aacc`, `#ff3333`, `#ffaa00`, `0x111122`, `0x4488aa`, `0x00ff88` across 19 source lines with semantic keys. Preserves the can-afford / can't-afford gameplay signal via textPrimary/textDisabled split.

Output: BuildSystem.js renders all menus from THEME.ui.*; stored item.color strings transit semantic .hex values transparently through pointer-restore paths; build passes.
</objective>

<execution_context>
@/Users/machado/Projects/bug-siege/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/bug-siege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cohesive-theme/06-CONTEXT.md
@.planning/phases/06-cohesive-theme/06-PATTERNS.md
@.planning/phases/06-cohesive-theme/06-01-SUMMARY.md
@src/systems/BuildSystem.js
@src/config/GameConfig.js

<interfaces>
<!-- Existing import (BuildSystem.js:1):
  import { TURRETS, GAME } from '../config/GameConfig.js';
  → becomes:
  import { TURRETS, GAME, THEME } from '../config/GameConfig.js';
-->

<!-- Pattern inventory for this file (06-PATTERNS.md "Call-Site Inventory → BuildSystem.js" lines 86-113):
  Pattern D (Rectangle + setStrokeStyle):  lines 100/102, 308/310
  Pattern B (text style color):             lines 108, 115 (×2), 127 (×2), 187 (×2), 209 (×2), 235 (×2), 258 (×2), 277, 316, 332
  Pattern C (hover pair):                   lines 131/132, 337/338
  Pattern H (flashDenied):                  line 349
  Pattern G (hover highlight ternary):     line 376 (×2)
  Pattern I (ternary text color for can-afford states): lines 115, 127, 187, 209, 235, 258 (all use same shape)
-->

<!-- IMPORTANT — stored color strings (06-PATTERNS.md lines 111-114):
  Line 338 (`text.setColor(item.color)`) and line 351 (`textObj.setColor(origColor)`) are NOT literals.
  They restore strings that were saved earlier. After migration, those saved strings will be
  `THEME.ui.*.hex` values — which is exactly what `setColor()` expects. No migration change
  needed at those lines; only the preceding literal-swap handles them.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate BuildSystem.js (24 replacements across 19 lines) to THEME.ui.*</name>
  <files>src/systems/BuildSystem.js</files>
  <read_first>
    - src/systems/BuildSystem.js (full file — be thorough; ~400 lines)
    - .planning/phases/06-cohesive-theme/06-PATTERNS.md section "`src/systems/BuildSystem.js` (19 literals — largest migration)" lines 86-113
    - .planning/phases/06-cohesive-theme/06-PATTERNS.md "Pattern B", "Pattern C", "Pattern D", "Pattern G", "Pattern H", "Pattern I"
    - .planning/phases/06-cohesive-theme/06-CONTEXT.md (D-06 semantic reuse: can-afford textPrimary/textDisabled split preserves gameplay signal)
    - .planning/phases/06-cohesive-theme/06-RESEARCH.md Pitfall #3 (always `.hex` or `.num` suffix, never bare object)
  </read_first>
  <action>
**Step 1 — Extend the import (line 1):**

Before:
```javascript
import { TURRETS, GAME } from '../config/GameConfig.js';
```

After:
```javascript
import { TURRETS, GAME, THEME } from '../config/GameConfig.js';
```

**Step 2 — Replace 24 literal occurrences across 19 source lines.** Apply the full migration table from 06-PATTERNS.md Call-Site Inventory for BuildSystem:

| Line | Before | After | Form |
|------|--------|-------|------|
| 100  | `add.rectangle(0, 0, menuWidth, menuHeight, 0x111122, 0.95)` (build menu bg) | `add.rectangle(0, 0, menuWidth, menuHeight, THEME.ui.surface.num, 0.95)` | `.num` |
| 102  | `.setStrokeStyle(2, 0x4488aa)` | `.setStrokeStyle(2, THEME.ui.surfaceBorder.num)` | `.num` |
| 108  | `color: '#ffdd00'` (credits header) | `color: THEME.ui.warning.hex` | `.hex` |
| 115  | `const color = canAfford ? '#ffffff' : '#555555';` | `const color = canAfford ? THEME.ui.textPrimary.hex : THEME.ui.textDisabled.hex;` | `.hex` (both branches) |
| 127  | `color: canAfford ? '#88aacc' : '#444444'` | `color: canAfford ? THEME.ui.accentSecondary.hex : THEME.ui.textDisabled.hex` | `.hex` (both branches) |
| 131  | `label.setColor('#00ff88')` (pointerover) | `label.setColor(THEME.ui.accentPrimary.hex)` | `.hex` |
| 132  | `label.setColor('#ffffff')` (pointerout — note: this is a literal-restore, not a saved-color-restore; migrate it) | `label.setColor(THEME.ui.textPrimary.hex)` | `.hex` |
| 187  | `color: canUpgrade ? '#ffffff' : '#555555'` | `color: canUpgrade ? THEME.ui.textPrimary.hex : THEME.ui.textDisabled.hex` | `.hex` (both) |
| 209  | `color: canRepair ? '#ffffff' : '#555555'` | `color: canRepair ? THEME.ui.textPrimary.hex : THEME.ui.textDisabled.hex` | `.hex` (both) |
| 235  | `color: canRepairType ? '#ffffff' : '#555555'` | `color: canRepairType ? THEME.ui.textPrimary.hex : THEME.ui.textDisabled.hex` | `.hex` (both) |
| 258  | `color: canRepairAll ? '#ffffff' : '#555555'` | `color: canRepairAll ? THEME.ui.textPrimary.hex : THEME.ui.textDisabled.hex` | `.hex` (both) |
| 277  | `color: '#ffaa00'` (Sell item — warning tone) | `color: THEME.ui.warning.hex` | `.hex` |
| 308  | `add.rectangle(0, 0, menuWidth, menuHeight, 0x111122, 0.95)` (turret menu bg) | `add.rectangle(0, 0, menuWidth, menuHeight, THEME.ui.surface.num, 0.95)` | `.num` |
| 310  | `.setStrokeStyle(2, 0x4488aa)` | `.setStrokeStyle(2, THEME.ui.surfaceBorder.num)` | `.num` |
| 316  | `color: '#ffdd00'` (turret menu credits) | `color: THEME.ui.warning.hex` | `.hex` |
| 332  | `color: '#88aacc'` (item description) | `color: THEME.ui.accentSecondary.hex` | `.hex` |
| 337  | `text.setColor('#00ff88')` (pointerover) | `text.setColor(THEME.ui.accentPrimary.hex)` | `.hex` |
| 349  | `textObj.setColor('#ff3333')` (flashDenied) | `textObj.setColor(THEME.ui.danger.hex)` | `.hex` |
| 376  | `const color = cell === 'empty' ? 0x00ff88 : 0x4488aa;` (hover highlight) | `const color = cell === 'empty' ? THEME.ui.accentPrimary.num : THEME.ui.surfaceBorder.num;` | `.num` (both) |

**Step 3 — Do NOT modify these "restore" lines (per 06-PATTERNS.md lines 111-114):**

- Line 338: `text.on('pointerout', () => text.setColor(item.color));` — `item.color` is a previously-saved string; after Step 2 it will be `THEME.ui.textPrimary.hex` or `THEME.ui.textDisabled.hex` (from lines 115/187/209/235/258) or `THEME.ui.warning.hex` (from line 277). Works transparently. Leave unchanged.
- Line 351 (inside flashDenied): `textObj.setColor(origColor);` — `origColor` was captured via `textObj.style.color` before Step 2's line-349 flash. Works transparently. Leave unchanged.

**Step 4 — Collapse rationale (D-06):**
- Every `#ffffff` → `ui.textPrimary` (default UI text)
- Every `#555555` / `#444444` → `ui.textDisabled` (can't-afford state — collapsed into one key; the near-identical shade drift is unified)
- Every `#00ff88` → `ui.accentPrimary` (hover / active)
- Every `#ffdd00` and `#ffaa00` → `ui.warning` (credits header, Sell item — both warm-caution semantics)
- Every `#88aacc` → `ui.accentSecondary` (informational labels)
- Every `0x111122` → `ui.surface` (menu panel fill — overrides UI-SPEC remap #111122 → #2d1b4e per D-03)
- Every `0x4488aa` → `ui.surfaceBorder` (menu panel border)
- `#ff3333` → `ui.danger` (flashDenied destructive signal)

**Guard rails (06-RESEARCH.md Pitfalls #1, #2, #3):**
- Lines 100, 102, 308, 310, 376 are numeric APIs → use `.num` only.
- All other migrated lines are text-style `color:` or `setColor()` → use `.hex` only.
- **Pitfall #3 (highest regression risk in this file):** do NOT write `THEME.ui.accentPrimary` bare (without `.hex`). The grep acceptance check below catches this.

**Verification-during-task:** after all replacements, grep the file yourself: `rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/systems/BuildSystem.js` must return zero matches before you consider the task done.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | grep -q error &amp;&amp; exit 1; rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/systems/BuildSystem.js &amp;&amp; (echo "hex literals remain in BuildSystem.js"; exit 1) || echo "file clean"; rg -c 'THEME\.ui\.' src/systems/BuildSystem.js | awk '$1&lt;20 {print "expected >=20 THEME.ui.* refs, got "$1; exit 1}'; rg 'THEME\.ui\.(accentPrimary|accentSecondary|warning|danger|success|textPrimary|textMuted|textDisabled|surface|surfaceBorder|hpBarBg|gridLine|loadingBar|loadingBarBg)\b(?!\.hex|\.num)' src/systems/BuildSystem.js -P &amp;&amp; (echo "bare THEME.ui.* object used without .hex/.num"; exit 1) || echo "all refs use .hex/.num suffix"</automated>
  </verify>
  <acceptance_criteria>
    - `rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/systems/BuildSystem.js` returns zero matches
    - `rg 'THEME\.ui\.' src/systems/BuildSystem.js` returns at least 20 matches (24 replacements is the theoretical max but some lines share references)
    - Every `THEME.ui.<key>` reference is followed by `.hex` or `.num` (grep for bare-object anti-pattern returns zero)
    - `rg 'THEME\.ui\.surface\.num' src/systems/BuildSystem.js` returns at least 2 matches (lines 100, 308)
    - `rg 'THEME\.ui\.surfaceBorder\.num' src/systems/BuildSystem.js` returns at least 3 matches (lines 102, 310, 376 occupied-branch)
    - `rg 'THEME\.ui\.accentPrimary' src/systems/BuildSystem.js` returns at least 3 matches (lines 131, 337, 376 empty-branch)
    - `rg 'THEME\.ui\.danger\.hex' src/systems/BuildSystem.js` returns exactly 1 match (line 349 flashDenied)
    - Line 338 `text.setColor(item.color)` and line 351 `textObj.setColor(origColor)` unchanged (grep for those exact strings returns 1 each)
    - `rg "import.*THEME.*from '\.\./config/GameConfig\.js'" src/systems/BuildSystem.js` returns 1 match
    - `npm run build` exits 0 with no warnings
  </acceptance_criteria>
  <done>
BuildSystem.js renders build/turret/upgrade/repair/sell menus from THEME.ui.*. Menu panels are cosmic-nebula (surface + surfaceBorder); can-afford labels flip between cosmic-accent and dim-nebula; hover states use cosmic purple; flashDenied pulses cosmic red; grid hover highlight distinguishes empty (purple) from occupied (nebula-blue). Stored `item.color` strings transit the restore paths unchanged. Build passes.
  </done>
</task>

</tasks>

<verification>
```bash
# Zero literals remaining
rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/systems/BuildSystem.js   # zero

# Reference density
rg 'THEME\.ui\.' src/systems/BuildSystem.js | wc -l   # ≥ 20

# Bare-object anti-pattern check (Pitfall #3)
rg 'THEME\.ui\.\w+(?!\.hex|\.num)(?=\W)' src/systems/BuildSystem.js -P
# → zero (every ref must use .hex or .num)

# Key-specific counts
rg 'THEME\.ui\.surface\.num' src/systems/BuildSystem.js       # ≥ 2 (lines 100, 308)
rg 'THEME\.ui\.surfaceBorder\.num' src/systems/BuildSystem.js # ≥ 3 (lines 102, 310, 376)
rg 'THEME\.ui\.accentPrimary' src/systems/BuildSystem.js      # ≥ 3
rg 'THEME\.ui\.danger\.hex' src/systems/BuildSystem.js        # = 1 (flashDenied)

# Stored-color restore lines preserved
rg 'text\.setColor\(item\.color\)' src/systems/BuildSystem.js       # = 1
rg 'textObj\.setColor\(origColor\)' src/systems/BuildSystem.js      # = 1

# Build
npm run build   # exit 0
```
</verification>

<success_criteria>
- BuildSystem.js consumes THEME.ui.* for all menu chrome (build, upgrade, repair, sell, flashDenied, grid hover)
- Zero hardcoded color literals remain
- 24 literal occurrences replaced across 19 source lines
- Stored-color restore paths (lines 338, 351) unchanged and work via transit
- Build passes with no warnings
- THEME-03 progress: 5 of 6 consumer files migrated
</success_criteria>

<output>
After completion, create `.planning/phases/06-cohesive-theme/06-06-SUMMARY.md` documenting:
- 24 replacements across 19 lines
- Confirmation restore paths untouched
- Call-out: `#555555` and `#444444` both collapsed to `ui.textDisabled` (D-06 unification)
- Build log outcome
</output>
