---
phase: 06-cohesive-theme
plan: 03
plan_id: 06-03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/scenes/BootScene.js
autonomous: true
requirements: [THEME-03]
tags: [theme, scene, refactor]

must_haves:
  truths:
    - "BootScene preloader progress-box fill uses THEME.ui.loadingBarBg.num"
    - "BootScene preloader 'Loading...' text color uses THEME.ui.accentPrimary.hex"
    - "BootScene preloader progress-bar fill uses THEME.ui.loadingBar.num"
    - "Procedural texture generators at lines 115-158 are completely untouched"
    - "npm run build exits 0 with no warnings"
  artifacts:
    - path: "src/scenes/BootScene.js"
      provides: "Preloader UI consuming THEME.ui palette"
      contains: "THEME.ui.loadingBar"
  key_links:
    - from: "src/scenes/BootScene.js preloader block"
      to: "src/config/GameConfig.js THEME.ui"
      via: "existing import statement (line 2 already has THEME)"
      pattern: "THEME\\.ui\\.loading"
---

<objective>
Migrate the 3 preloader color literals in `src/scenes/BootScene.js` (lines 19, 27, 32) to reference `THEME.ui.*` constants. The existing `import { BUGS, GRID, GAME, THEME }` at line 2 already pulls THEME in — no import change needed. Procedural texture generators at lines 115-158 stay completely untouched (D-01 boundary; those are game-entity pixel data, not UI chrome).

Purpose: Align the loading screen with the cosmic palette. Replace green `#00ff88` (Loading text) and green `0x00ff88` (progress bar fill) — the only UI-chrome leaks in BootScene — with `ui.accentPrimary` and `ui.loadingBar`. Upgrade the dim `0x222222` preloader backdrop to `ui.loadingBarBg` for consistency with the HP-bar recessed-track shade.

Output: BootScene preloader renders from THEME.ui.*; texture generators unmodified; build passes.
</objective>

<execution_context>
@/Users/machado/Projects/bug-siege/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/bug-siege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cohesive-theme/06-CONTEXT.md
@.planning/phases/06-cohesive-theme/06-PATTERNS.md
@.planning/phases/06-cohesive-theme/06-01-SUMMARY.md
@src/scenes/BootScene.js
@src/config/GameConfig.js

<interfaces>
<!-- Existing import at BootScene.js:2 (no change needed):
  import { BUGS, GRID, GAME, THEME } from '../config/GameConfig.js';
-->

<!-- Migration targets (06-PATTERNS.md Call-Site Inventory → BootScene.js preloader only):
  Line 19: progressBox.fillStyle(0x222222, 0.8)      → THEME.ui.loadingBarBg.num
  Line 27: text style `color: '#00ff88'`             → THEME.ui.accentPrimary.hex
  Line 32: progressBar.fillStyle(0x00ff88, 1)        → THEME.ui.loadingBar.num
-->

<!-- CRITICAL: Lines 115-158 (generateParticleTextures, generateFallback) are OUT OF SCOPE.
  These generate sprite/texture pixel data (0xffffff particle fill, 0xff00ff magenta fallback,
  0x1a1a2e background fallback). Per D-01 and 06-PATTERNS.md line 51, leave as-is.
  Line 91 `ctx.fillStyle = THEME.background` was migrated in Phase 1 — do not touch.
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate BootScene.js preloader (3 literals) to THEME.ui.*</name>
  <files>src/scenes/BootScene.js</files>
  <read_first>
    - src/scenes/BootScene.js (lines 1-40 — preloader block; also skim lines 115-158 to confirm you do NOT touch them)
    - .planning/phases/06-cohesive-theme/06-PATTERNS.md section "`src/scenes/BootScene.js` preloader only (3 literals)" lines 43-53
    - .planning/phases/06-cohesive-theme/06-PATTERNS.md "Pattern F" lines 313-343
    - .planning/phases/06-cohesive-theme/06-CONTEXT.md (D-01 boundary — texture generators out of scope)
  </read_first>
  <action>
The import at line 2 already has `THEME` — verify first, do NOT add another import line.

Apply the following replacements. Do NOT touch any other line.

**Line 19** — progress-box fill (numeric API, `.num` form):

Before:
```javascript
progressBox.fillStyle(0x222222, 0.8);
```

After:
```javascript
progressBox.fillStyle(THEME.ui.loadingBarBg.num, 0.8);
```

**Line 27** — "Loading..." text style color (string API, `.hex` form):

Before:
```javascript
color: '#00ff88',
```

After:
```javascript
color: THEME.ui.accentPrimary.hex,
```

**Line 32** — progress-bar fill inside `load.on('progress')` (numeric API, `.num` form):

Before:
```javascript
progressBar.fillStyle(0x00ff88, 1);
```

After:
```javascript
progressBar.fillStyle(THEME.ui.loadingBar.num, 1);
```

**CRITICAL carve-outs (do NOT modify):**

- Lines 115-158 — `generateParticleTextures()` and `generateFallback()` bodies. These draw to `Phaser.GameObjects.Graphics` to bake sprite textures; the literals (`0xffffff`, `0xff00ff`, `0x1a1a2e`, etc.) are per-pixel texture data, not UI chrome. Leave untouched per D-01 and 06-PATTERNS.md line 51.
- Line 91 `ctx.fillStyle = THEME.background` — already migrated in Phase 1. Leave as-is.
- Any other literal between lines 40 and 115 (if present) that is part of the texture generation pipeline.

**Guard rails (06-RESEARCH.md Pitfalls):**
- Pitfall #1: Lines 19 and 32 are `graphics.fillStyle(color, alpha)` — takes a number → use `.num`.
- Pitfall #2: Line 27 is a text style `color:` property — takes a CSS string → use `.hex`.
- Pitfall #3: Do NOT write `THEME.ui.loadingBar` (bare object) — always suffix `.hex` or `.num`.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | grep -q error &amp;&amp; exit 1; rg -n '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/scenes/BootScene.js | awk -F: '$2&gt;=15 &amp;&amp; $2&lt;=40 {print; exit 1}' &amp;&amp; (echo "literals remain in preloader block"; exit 1) || echo "preloader clean"; rg -c 'THEME\.ui\.' src/scenes/BootScene.js | grep -qE '^[3-9]$|^1[0-9]$' || (echo "expected >=3 THEME.ui.* refs"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/scenes/BootScene.js` returns only matches in the texture-generator region (lines >= ~91) — no matches in the preloader block (lines 15-40)
    - `rg 'THEME\.ui\.' src/scenes/BootScene.js` returns at least 3 matches (loadingBarBg, accentPrimary, loadingBar)
    - `rg 'THEME\.ui\.loadingBarBg\.num' src/scenes/BootScene.js` returns 1 match
    - `rg 'THEME\.ui\.loadingBar\.num' src/scenes/BootScene.js` returns 1 match
    - `rg 'THEME\.ui\.accentPrimary\.hex' src/scenes/BootScene.js` returns 1 match
    - Lines 115-158 diff is empty (no accidental edits to texture generators)
    - `npm run build` exits 0 with no warnings
  </acceptance_criteria>
  <done>
BootScene.js preloader renders progress box (recessed nebula), progress bar (cosmic purple fill), and Loading text (cosmic purple) from THEME.ui.*. Texture generators at lines 115-158 completely unchanged. Build passes. Visual: loading screen shows purple progress bar against dark nebula backdrop, no green accent.
  </done>
</task>

</tasks>

<verification>
```bash
# Preloader block should be clean
rg -n '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/scenes/BootScene.js | awk -F: '$2 <= 40'
# → expect zero

# Texture generators should be untouched
git diff src/scenes/BootScene.js | grep -E '^[+-].*0x(ffffff|ff00ff|1a1a2e)'
# → expect zero (no changes to those literals)

# Migration references
rg 'THEME\.ui\.(loadingBar|accentPrimary)' src/scenes/BootScene.js
# → expect 3 matches

# Build
npm run build   # exit 0
```
</verification>

<success_criteria>
- BootScene preloader (lines 15-40) contains zero hardcoded color literals
- Texture generators (lines 115-158) untouched — git diff confirms no edits in that region
- THEME already imported at line 2 (no import edit needed)
- 3 literals replaced: line 19 (loadingBarBg), line 27 (accentPrimary), line 32 (loadingBar)
- Build passes with no warnings
- THEME-03 progress: 2 of 6 consumer files migrated
</success_criteria>

<output>
After completion, create `.planning/phases/06-cohesive-theme/06-03-SUMMARY.md` documenting:
- 3 literal replacements in preloader
- Confirmation that texture generators (lines 115-158) were not modified
- Confirmation import was already present
- Build log outcome
</output>
