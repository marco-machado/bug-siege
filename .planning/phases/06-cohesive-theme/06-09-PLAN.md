---
phase: 06-cohesive-theme
plan: 09
plan_id: 06-09
type: execute
wave: 3
depends_on: [06-01, 06-02, 06-03, 06-04, 06-05, 06-06, 06-07, 06-08]
files_modified: []
autonomous: true
requirements: [THEME-03]
tags: [theme, verification, gate]

must_haves:
  truths:
    - "D-07 grep gate returns zero unexpected hex/numeric color literals across src/"
    - "Exactly 14 THEME.ui.* semantic keys exist in GameConfig.js, each with {hex, num} pair"
    - "THEME.ui is consumed in all 7 consumer files (MainMenuScene, BootScene, UIScene, GameOverScene, BuildSystem, Turret, GameScene) plus main.js"
    - "All documented VFX/texture carve-outs remain intact (BootScene textures, Turret tints, GameScene setTintFill)"
    - "npm run build exits 0 with no warnings"
    - "ROADMAP.md Phase 6 Success Criteria #1 and #2 satisfied"
  artifacts: []
  key_links:
    - from: "verification sweep"
      to: "THEME-03 completion"
      via: "D-07 grep gate + build + semantic count + dual-format integrity"
      pattern: "all gates green"
---

<objective>
Run the D-07 verification gate end-to-end. Confirm:
- Gate A: zero unexpected hex/numeric color literals across `src/` outside documented carve-outs.
- Gate B: `npm run build` exits 0 with no warnings.
- Gate D: exactly 14 semantic `ui.*` keys in GameConfig.js.
- Gate E: every `ui.*` entry has both `hex` and `num` sub-keys.
- Coverage: THEME is imported in all 8 migration files.
- Carve-out integrity: BootScene texture generators (lines 115-158), Turret VFX tints (type-color overlays, 0xffdd44 upgrade tint), and GameScene core damage flash (0xff4444 setTintFill) are unchanged.

This plan creates no files and modifies no production code. It is a pure verification sweep. If any gate fails, the executor surfaces the specific violating file/line and halts for planner triage (re-entry via /gsd-plan-phase --gaps).

Purpose: Falsifiable phase completion per D-07. No visual smoke-test inside this plan — that happens in /gsd-verify-work (Gate C), which the solo-dev user drives manually in browser.

Output: A gate report (in the summary) confirming THEME-03 is complete, or a specific defect list identifying gaps to close.
</objective>

<execution_context>
@/Users/machado/Projects/bug-siege/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/bug-siege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cohesive-theme/06-CONTEXT.md
@.planning/phases/06-cohesive-theme/06-RESEARCH.md
@.planning/phases/06-cohesive-theme/06-PATTERNS.md
@.planning/phases/06-cohesive-theme/06-01-SUMMARY.md
@.planning/phases/06-cohesive-theme/06-02-SUMMARY.md
@.planning/phases/06-cohesive-theme/06-03-SUMMARY.md
@.planning/phases/06-cohesive-theme/06-04-SUMMARY.md
@.planning/phases/06-cohesive-theme/06-05-SUMMARY.md
@.planning/phases/06-cohesive-theme/06-06-SUMMARY.md
@.planning/phases/06-cohesive-theme/06-07-SUMMARY.md
@.planning/phases/06-cohesive-theme/06-08-SUMMARY.md

<interfaces>
<!-- Documented carve-outs from Plans 01-08 (leave untouched — verify they still exist):

  1. src/config/GameConfig.js lines 142-212 — VFX and POSTFX frozen configs (intentionally contain 0x... literals).
  2. src/scenes/BootScene.js lines 115-158 — generateParticleTextures() and generateFallback() (texture pixel data, not UI chrome).
  3. src/scenes/MainMenuScene.js line 74 — g.fillStyle(0xffffff, alpha) inside createStarfield() (procedural game-world art).
  4. src/scenes/UIScene.js line 118 — backgroundColor: '#000000aa' (8-digit RGBA debug overlay, excluded by 6-digit grep anchor).
  5. src/scenes/GameScene.js line 291 — this.coreSprite.setTintFill(0xff4444) (core damage flash VFX).
  6. src/entities/Turret.js — type-color overlays, 0xffdd44 upgrade tint, 0xff4444 damage flash (VFX semantics outside lines 53/55/405-407).
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Run Gate A (D-07 grep — no rogue color literals)</name>
  <files></files>
  <read_first>
    - .planning/phases/06-cohesive-theme/06-CONTEXT.md (D-07 definition, lines 61-62)
    - .planning/phases/06-cohesive-theme/06-RESEARCH.md "Validation Architecture" (lines 432-463)
    - .planning/phases/06-cohesive-theme/06-PATTERNS.md documented carve-outs (throughout — BootScene lines 115-158, MainMenu line 74, UIScene line 118, etc.)
  </read_first>
  <action>
Run the D-07 grep gate with carve-outs filtered. The target command is:

```
rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/ \
  -g '!src/config/GameConfig.js'
```

Expected gross matches: the remaining carve-outs from the Interfaces section above:

1. `src/scenes/BootScene.js` — lines in the 91-158 range (texture generators: `0xffffff` particles, `0xff00ff` magenta fallback, `0x1a1a2e` background fallback, any other texture-pixel literal in that range). Acceptable.
2. `src/scenes/MainMenuScene.js:74` — `g.fillStyle(0xffffff, alpha)` inside createStarfield(). Acceptable.
3. `src/scenes/GameScene.js:291` — `setTintFill(0xff4444)` core damage flash. Acceptable.
4. `src/entities/Turret.js` — VFX tints outside HP-bar lines (type-color overlays, `0xffdd44` upgrade tint, other damage-flash literals that are NOT lines 53/55/405-407). Acceptable.

Filter the command output to isolate UNEXPECTED hits. Run this sequence and record results:

```
rg -n '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/ \
  -g '!src/config/GameConfig.js' \
  > /tmp/raw-hits.txt

# Classify each line:
# - In a documented carve-out region → OK
# - Anywhere else → DEFECT
cat /tmp/raw-hits.txt
```

For each hit, classify it against the carve-out list. If any hit does NOT match a carve-out:
- Record the file, line number, and literal
- Stop this task with exit code 1
- Surface the defect list in the task's verify block output and in the plan summary

If ALL hits match documented carve-outs, the gate passes for Gate A.

Special-case (Turret.js): because Turret.js has both in-scope HP-bar literals (now migrated) and out-of-scope VFX tints (retained), classify each Turret.js hit by line number. Any hit at line 53, 55, 405, 406, 407 is a DEFECT (Plan 07 should have removed them). Any hit elsewhere in Turret.js is the expected VFX carve-out and is OK.

Special-case (UIScene.js): line 118 `'#000000aa'` is an 8-digit CSS RGBA literal; the 6-digit-anchored pattern `#[0-9a-fA-F]{6}` DOES match the first 6 hex chars of an 8-digit string. If this literal shows up in the grep output, confirm it's at line 118 and the full literal is 8-digit RGBA → OK. A 6-digit standalone `#000000` elsewhere would be a DEFECT.

Output from this task: either "Gate A: PASS (N carve-out hits, all classified)" with the classified list, or "Gate A: FAIL" with a defect list.
  </action>
  <verify>
    <automated>rg -n '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/ -g '!src/config/GameConfig.js' | tee /tmp/gate-a-hits.txt; rg -n '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/scenes/MainMenuScene.js -g '!src/config/GameConfig.js' | rg -v ':74:' &amp;&amp; (echo "MainMenu leak"; exit 1) || true; rg -n '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/scenes/GameOverScene.js &amp;&amp; (echo "GameOverScene leak"; exit 1) || true; rg -n '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/systems/BuildSystem.js &amp;&amp; (echo "BuildSystem leak"; exit 1) || true; rg -n '(0x333333|0x00ff00)' src/entities/Turret.js &amp;&amp; (echo "Turret HP-bar leak"; exit 1) || true; rg -n "'#ff8844'" src/scenes/GameScene.js &amp;&amp; (echo "GameScene wave-announce leak"; exit 1) || true; rg -n "'#0a0a12'" src/main.js &amp;&amp; (echo "main.js leak"; exit 1) || true; rg -n '#[0-9a-fA-F]{6}[^a-fA-F]' src/scenes/UIScene.js &amp;&amp; (echo "UIScene leak (6-digit only)"; exit 1) || true; echo "Gate A: PASS"</automated>
  </verify>
  <acceptance_criteria>
    - GameOverScene.js: zero hex/numeric literal hits
    - BuildSystem.js: zero hex/numeric literal hits
    - MainMenuScene.js: only line-74 starfield `0xffffff` remains (1 hit OK)
    - UIScene.js: zero 6-digit hex/numeric literal hits (8-digit `'#000000aa'` at line 118 is the only literal, excluded by 6-digit anchor)
    - Turret.js: zero hits in lines 53, 55, 405, 406, 407; other VFX tints (e.g. `0xffdd44` upgrade, type-color overlays) may remain (documented D-01 carve-out)
    - GameScene.js: zero `'#ff8844'` hits; `0xff4444` at line 291 `setTintFill` is OK (documented carve-out)
    - BootScene.js: zero hits in preloader block (lines 15-40); texture-generator hits in lines 91-158 are OK (documented carve-out)
    - main.js: zero `'#0a0a12'` literal hits
  </acceptance_criteria>
  <done>
Gate A produced either PASS with a documented carve-out list, or FAIL with a specific defect list. On FAIL, halt the plan and surface defects for /gsd-plan-phase --gaps triage.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Run Gates B, D, E (build + semantic count + dual-format integrity)</name>
  <files></files>
  <read_first>
    - .planning/phases/06-cohesive-theme/06-RESEARCH.md "Validation Architecture → Phase Requirements → Test Map" (lines 447-453)
    - .planning/phases/06-cohesive-theme/06-01-SUMMARY.md (expected 14 keys with the finalized hex/num values)
  </read_first>
  <action>
Run three gates in sequence. Record the output of each.

**Gate B — Build gate:**

```
npm run build
echo "EXIT_CODE=$?"
```

Expected: exit 0 with no warnings in stdout/stderr. If warnings or errors appear, capture them verbatim. A failing build is a hard fail.

**Gate D — Semantic key count (≤14 per D-06 ceiling):**

```
rg '^\s+\w+:\s*Object\.freeze\(\{ hex:' src/config/GameConfig.js | wc -l
```

Expected: exactly 14.

- If < 14: a key was dropped from Plan 01 → defect.
- If > 14: someone added a 15th key (suggesting the GameScene wave-announcement was migrated to a new `ui.waveAnnouncement` instead of `ui.warning`) → defect per D-06.

**Gate E — Dual-format integrity (every ui.* entry has both hex and num):**

```
rg "Object\.freeze\(\{ hex: '#[0-9a-fA-F]{6}', num: 0x[0-9a-fA-F]{6} \}\)" src/config/GameConfig.js | wc -l
```

Expected: exactly 14 (matches Gate D count — confirms no entry is missing either sub-key).

Also verify each hex/num pair is consistent by checking the full THEME.ui block visually — for each key, `.hex` should equal `#` + hex representation of `.num`. This is a sanity check, not an automated grep.

If any gate fails, record the failure and halt.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | tee /tmp/gate-b.log; grep -qE '(error|warning)' /tmp/gate-b.log &amp;&amp; (echo "Gate B: FAIL — build produced errors/warnings"; exit 1) || echo "Gate B: PASS"; COUNT=$(rg '^\s+\w+:\s*Object\.freeze\(\{ hex:' src/config/GameConfig.js | wc -l | tr -d ' '); [ "$COUNT" = "14" ] &amp;&amp; echo "Gate D: PASS (14 keys)" || (echo "Gate D: FAIL (got $COUNT, expected 14)"; exit 1); PAIRS=$(rg "Object\.freeze\(\{ hex: '#[0-9a-fA-F]{6}', num: 0x[0-9a-fA-F]{6} \}\)" src/config/GameConfig.js | wc -l | tr -d ' '); [ "$PAIRS" = "14" ] &amp;&amp; echo "Gate E: PASS (14 hex/num pairs)" || (echo "Gate E: FAIL (got $PAIRS, expected 14)"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - Gate B: npm run build exits 0 with no warnings (grep for "warning" and "error" in build log returns zero)
    - Gate D: exactly 14 ui.* keys in GameConfig.js
    - Gate E: exactly 14 entries match the full `{ hex, num }` shape
    - All three gates pass together — a single failure halts the task
  </acceptance_criteria>
  <done>
Gates B, D, and E green. Config layer verified sound.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Coverage audit — THEME imported in all 8 migration files</name>
  <files></files>
  <read_first>
    - Plans 01-08 file lists (summarized: GameConfig.js, MainMenuScene.js, BootScene.js, UIScene.js, GameOverScene.js, BuildSystem.js, Turret.js, main.js, GameScene.js)
  </read_first>
  <action>
Confirm that every file touched by Plans 02-08 imports THEME. This catches cases where a plan's build succeeded (THEME wasn't used, so import wasn't needed) but the migration wasn't actually applied.

Run:

```
for f in \
  src/scenes/MainMenuScene.js \
  src/scenes/BootScene.js \
  src/scenes/UIScene.js \
  src/scenes/GameOverScene.js \
  src/systems/BuildSystem.js \
  src/entities/Turret.js \
  src/scenes/GameScene.js \
  src/main.js; do
    rg -q 'import.*THEME.*GameConfig' "$f" && echo "OK: $f" || echo "MISSING: $f"
done
```

Expected: all 8 files report "OK". Any "MISSING" is a defect — the file's migration was not applied.

Additionally, count THEME.ui.* references per file for plausibility:

```
for f in \
  src/scenes/MainMenuScene.js \
  src/scenes/BootScene.js \
  src/scenes/UIScene.js \
  src/scenes/GameOverScene.js \
  src/systems/BuildSystem.js \
  src/entities/Turret.js \
  src/scenes/GameScene.js; do
    CT=$(rg -c 'THEME\.ui\.' "$f" 2>/dev/null || echo 0)
    echo "$f: $CT THEME.ui.* refs"
done
```

Expected minimums (from PATTERNS.md inventory):
- MainMenuScene.js: ≥ 7
- BootScene.js: ≥ 3
- UIScene.js: ≥ 9
- GameOverScene.js: ≥ 8
- BuildSystem.js: ≥ 20
- Turret.js: ≥ 5
- GameScene.js: ≥ 1

main.js uses `THEME.background` (no `.ui.` prefix) so the ui-count grep returns 0 for it — that's expected. Separately confirm `rg 'THEME\.background' src/main.js` returns ≥ 1.

If any count falls below its minimum, flag the file as under-migrated and halt.
  </action>
  <verify>
    <automated>for f in src/scenes/MainMenuScene.js src/scenes/BootScene.js src/scenes/UIScene.js src/scenes/GameOverScene.js src/systems/BuildSystem.js src/entities/Turret.js src/scenes/GameScene.js src/main.js; do rg -q 'import.*THEME.*GameConfig' "$f" || (echo "MISSING THEME import: $f"; exit 1); done; echo "All 8 files import THEME"; for pair in "src/scenes/MainMenuScene.js:7" "src/scenes/BootScene.js:3" "src/scenes/UIScene.js:9" "src/scenes/GameOverScene.js:8" "src/systems/BuildSystem.js:20" "src/entities/Turret.js:5" "src/scenes/GameScene.js:1"; do F="${pair%:*}"; MIN="${pair##*:}"; CT=$(rg -c 'THEME\.ui\.' "$F" 2>/dev/null || echo 0); [ "$CT" -ge "$MIN" ] &amp;&amp; echo "$F: $CT refs (≥ $MIN OK)" || (echo "$F: $CT refs, expected ≥ $MIN"; exit 1); done; rg -q 'THEME\.background' src/main.js &amp;&amp; echo "main.js references THEME.background OK" || (echo "main.js missing THEME.background"; exit 1); echo "Coverage audit: PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 files (MainMenuScene, BootScene, UIScene, GameOverScene, BuildSystem, Turret, GameScene, main) have a THEME import
    - Per-file THEME.ui.* reference counts meet their minimums
    - main.js references THEME.background at least once
    - No "MISSING" or "under minimum" defects reported
  </acceptance_criteria>
  <done>
All migration surfaces import THEME and consume it in expected densities. THEME-03 coverage confirmed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Carve-out integrity check — VFX/texture literals preserved</name>
  <files></files>
  <read_first>
    - .planning/phases/06-cohesive-theme/06-CONTEXT.md (D-01 carve-outs)
    - .planning/phases/06-cohesive-theme/06-PATTERNS.md (lines 51, 69-70, 111-114, and other carve-out notes)
  </read_first>
  <action>
Confirm that the documented carve-outs were NOT accidentally migrated. A plan that over-migrated (e.g., someone changed `setTintFill(0xff4444)` to `THEME.ui.danger.num`) breaks the D-01 boundary and destroys gameplay VFX semantics.

Check each carve-out exists:

```
# BootScene texture generator (expect texture-pixel literals to still be there)
rg -c '0xff00ff' src/scenes/BootScene.js    # magenta fallback — expect ≥ 1
rg -c '0x1a1a2e' src/scenes/BootScene.js    # background fallback texture pixel — expect ≥ 1

# MainMenu starfield
rg -n '0xffffff' src/scenes/MainMenuScene.js    # expect line 74 hit

# GameScene core damage flash
rg -n 'setTintFill\(0xff4444\)' src/scenes/GameScene.js    # expect 1 hit

# UIScene debug overlay (8-digit RGBA)
rg -n "backgroundColor: '#000000aa'" src/scenes/UIScene.js    # expect 1 hit at line 118

# Turret VFX upgrade tint
rg -n '0xffdd44' src/entities/Turret.js    # expect ≥ 1 hit (outside lines 53/55/405-407)

# GameConfig VFX/POSTFX blocks
rg -c '0x[0-9a-fA-F]{6}' src/config/GameConfig.js    # expect ≥ 20 (VFX/POSTFX numeric colors + THEME.ui 14 num entries)
```

Any carve-out that returns zero is a defect — the carve-out was accidentally removed.

Cross-check Turret.js: the HP-bar lines (53, 55, 405-407) should now have THEME.ui.* references, but other VFX literals (type-color overlays, upgrade tint, damage-flash) should remain intact:

```
rg -n '0x' src/entities/Turret.js | head -30
```

Expected: hits outside lines 53, 55, 405-407 (VFX tints, type-color logic). If hits appear AT lines 53, 55, 405, 406, 407, Plan 07 under-migrated.
  </action>
  <verify>
    <automated>rg -c '0xff00ff' src/scenes/BootScene.js | grep -qE '^[1-9]' || (echo "BootScene fallback texture missing"; exit 1); rg -c '0x1a1a2e' src/scenes/BootScene.js | grep -qE '^[1-9]' || (echo "BootScene bg fallback texture missing"; exit 1); rg -n '0xffffff' src/scenes/MainMenuScene.js | grep -q ':74:' || (echo "MainMenu starfield carve-out missing"; exit 1); rg -n 'setTintFill\(0xff4444\)' src/scenes/GameScene.js | wc -l | grep -q '^[[:space:]]*1' || (echo "GameScene damage flash carve-out missing"; exit 1); rg -c "backgroundColor: '#000000aa'" src/scenes/UIScene.js | grep -qE '^[1-9]' || (echo "UIScene debug overlay carve-out missing"; exit 1); rg -c '0xffdd44' src/entities/Turret.js | grep -qE '^[1-9]' || (echo "Turret upgrade tint missing"; exit 1); rg -n '0x(333333|00ff00|ff3333|ffaa00)' src/entities/Turret.js | grep -E ':(53|55|405|406|407):' &amp;&amp; (echo "Turret HP bar NOT migrated at lines 53/55/405-407"; exit 1) || echo "Turret HP bar migration verified"; echo "Carve-out integrity: PASS"</automated>
  </verify>
  <acceptance_criteria>
    - BootScene.js retains `0xff00ff` (magenta fallback) and `0x1a1a2e` (bg texture) literals in generator region
    - MainMenuScene.js:74 still contains `0xffffff` (starfield)
    - GameScene.js line 291 still has `setTintFill(0xff4444)` (1 hit exactly)
    - UIScene.js line 118 still has `'#000000aa'` (debug overlay)
    - Turret.js still has `0xffdd44` upgrade tint (outside HP-bar lines)
    - Turret.js has NO hits for `0x333333`, `0x00ff00`, `0xff3333`, `0xffaa00` at lines 53, 55, 405, 406, 407 specifically
    - GameConfig.js retains ≥ 20 numeric hex literals (VFX + POSTFX + ui.num entries)
  </acceptance_criteria>
  <done>
All D-01 carve-outs verified intact. No over-migration. Phase boundaries between THEME-03 (UI chrome) and VFX/POSTFX (gameplay effects) preserved.
  </done>
</task>

</tasks>

<verification>
All four task's automated gates pass. Specifically:

```bash
# Gate A — no rogue UI-chrome literals
rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/scenes/GameOverScene.js   # zero
rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/systems/BuildSystem.js    # zero
rg "'#ff8844'" src/scenes/GameScene.js                                # zero
rg "'#0a0a12'" src/main.js                                            # zero
rg '(0x333333|0x00ff00)' src/entities/Turret.js                       # zero

# Gate B — build
npm run build   # exit 0, no warnings

# Gate D — semantic count
rg '^\s+\w+:\s*Object\.freeze\(\{ hex:' src/config/GameConfig.js | wc -l    # = 14

# Gate E — dual-format integrity
rg "Object\.freeze\(\{ hex: '#[0-9a-fA-F]{6}', num: 0x[0-9a-fA-F]{6} \}\)" src/config/GameConfig.js | wc -l   # = 14

# Coverage — THEME imports
for f in src/scenes/{MainMenu,Boot,UI,GameOver,Game}Scene.js src/systems/BuildSystem.js src/entities/Turret.js src/main.js; do
  rg -q 'import.*THEME.*GameConfig' "$f" && echo OK || echo MISSING
done
# expect 8 OK

# Carve-outs still intact
rg -n 'setTintFill\(0xff4444\)' src/scenes/GameScene.js               # = 1
rg -n "backgroundColor: '#000000aa'" src/scenes/UIScene.js            # = 1
rg -n '0xffffff' src/scenes/MainMenuScene.js | grep :74:              # 1 hit
rg -c '0xffdd44' src/entities/Turret.js                               # ≥ 1
```

Gate C (visual smoke-test) is NOT in this plan — it runs in /gsd-verify-work and requires the user to open localhost:5173 in a browser.
</verification>

<success_criteria>
- Gate A: no rogue hex/numeric UI-chrome literals in any migrated file
- Gate B: npm run build exits 0 with no warnings
- Gate D: exactly 14 THEME.ui.* semantic keys
- Gate E: every ui.* entry has matching `{ hex, num }` pair
- Coverage: 8 migration files import THEME
- Carve-outs: VFX tints, texture-generator literals, debug overlay, starfield, core damage flash all intact
- ROADMAP.md Phase 6 Success Criteria #1 satisfied ("All hardcoded color values across 6+ files reference THEME config constants")
- ROADMAP.md Phase 6 Success Criteria #2 ready for Gate C manual verification in /gsd-verify-work ("consistent cosmic nebula palette")
- THEME-03 complete; phase ready to close out
</success_criteria>

<output>
After completion, create `.planning/phases/06-cohesive-theme/06-09-SUMMARY.md` documenting:
- Gate A result (PASS / FAIL with defect list if failed)
- Gate B result (build log summary)
- Gate D and Gate E counts (actual vs expected)
- Coverage audit per-file THEME.ui.* reference counts
- Carve-out integrity check per-file outcomes
- Overall phase completion statement: "THEME-03 ready for /gsd-verify-work manual Gate C"
</output>
