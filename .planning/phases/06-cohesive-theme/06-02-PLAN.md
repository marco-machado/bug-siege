---
phase: 06-cohesive-theme
plan: 02
plan_id: 06-02
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/scenes/MainMenuScene.js
autonomous: true
requirements: [THEME-03]
tags: [theme, scene, refactor]

must_haves:
  truths:
    - "MainMenuScene imports THEME from GameConfig.js"
    - "All 7 hardcoded UI-chrome color literals in MainMenuScene.js are replaced with THEME.ui.* references"
    - "Grid overlay, title, subtitle, start button (default + hover states), and footer all render from THEME.ui palette"
    - "npm run build exits 0 with no warnings"
  artifacts:
    - path: "src/scenes/MainMenuScene.js"
      provides: "Main menu UI consuming THEME.ui palette"
      contains: "THEME.ui.accentPrimary.hex"
  key_links:
    - from: "src/scenes/MainMenuScene.js"
      to: "src/config/GameConfig.js"
      via: "import { GAME, POSTFX, THEME } from '../config/GameConfig.js'"
      pattern: "import.*THEME.*from.*GameConfig"
---

<objective>
Migrate all 7 hardcoded UI-chrome color literals in `src/scenes/MainMenuScene.js` to reference `THEME.ui.*` constants. Apply the text-style / setColor / lineStyle patterns from 06-PATTERNS.md verbatim. No structural changes — only literal replacements and a one-line import extension.

Purpose: Unify main menu chrome (grid, title, subtitle, start button, footer) under the cosmic nebula palette. Removes the green `#00ff88` that breaks aesthetic hardest (per 06-CONTEXT.md specifics) and collapses `#ffffff`/`#668899`/`#445566`/`#334455` drift into semantic keys.

Output: MainMenuScene.js renders from THEME.ui.*; build passes; file contains zero hex/numeric color literals outside the import statement.
</objective>

<execution_context>
@/Users/machado/Projects/bug-siege/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/bug-siege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cohesive-theme/06-CONTEXT.md
@.planning/phases/06-cohesive-theme/06-PATTERNS.md
@.planning/phases/06-cohesive-theme/06-01-SUMMARY.md
@src/scenes/MainMenuScene.js
@src/config/GameConfig.js

<interfaces>
<!-- THEME.ui shape (from 06-01 output — must exist before this plan runs):
  textPrimary / accentPrimary / textMuted / gridLine — each exposes .hex (CSS string) and .num (packed int)
-->

<!-- Existing import at MainMenuScene.js:2:
  import { GAME, POSTFX } from '../config/GameConfig.js';
  → becomes:
  import { GAME, POSTFX, THEME } from '../config/GameConfig.js';
-->

<!-- API-to-form rule (06-PATTERNS.md "Shared Patterns → API-to-form rule"):
  graphics.lineStyle(w, color, alpha)  → .num
  text style `color:` property         → .hex
  text.setColor(...)                   → .hex
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate MainMenuScene.js 7 literals to THEME.ui.*</name>
  <files>src/scenes/MainMenuScene.js</files>
  <read_first>
    - src/scenes/MainMenuScene.js (full file — ~80 lines)
    - .planning/phases/06-cohesive-theme/06-PATTERNS.md section "`src/scenes/MainMenuScene.js` (7 literals)" lines 29-41 — exact per-line migration table
    - .planning/phases/06-cohesive-theme/06-PATTERNS.md sections "Pattern B", "Pattern C", "Pattern F (variant)", "Shared Patterns → Import augmentation"
    - .planning/phases/06-cohesive-theme/06-CONTEXT.md (D-01 boundary, D-04 inline-both, D-06 semantic reuse)
  </read_first>
  <action>
Apply the following replacements in `src/scenes/MainMenuScene.js`. Do NOT touch any line not listed. Do NOT touch `createStarfield()` or `g.fillStyle(0xffffff, alpha)` at line 74 (procedural starfield — out of scope per D-01, confirmed 06-PATTERNS.md line 41).

**Step 1 — Extend the import (line 2):**

Before:
```javascript
import { GAME, POSTFX } from '../config/GameConfig.js';
```

After:
```javascript
import { GAME, POSTFX, THEME } from '../config/GameConfig.js';
```

**Step 2 — Replace the 7 literals** (per 06-PATTERNS.md Call-Site Inventory for MainMenuScene):

| Line | Before (literal) | After (replacement) | API form |
|------|------------------|---------------------|----------|
| 23   | `gridG.lineStyle(1, 0x334455, 0.15)` | `gridG.lineStyle(1, THEME.ui.gridLine.num, 0.15)` | `.num` (Pattern F variant) |
| 30   | `color: '#00ff88'` (title) | `color: THEME.ui.accentPrimary.hex` | `.hex` (Pattern B) |
| 37   | `color: '#668899'` (subtitle) | `color: THEME.ui.textMuted.hex` | `.hex` (Pattern B) |
| 44   | `color: '#ffffff'` (start button default) | `color: THEME.ui.textPrimary.hex` | `.hex` (Pattern B) |
| 47   | `startBtn.setColor('#00ff88')` (pointerover) | `startBtn.setColor(THEME.ui.accentPrimary.hex)` | `.hex` (Pattern C) |
| 48   | `startBtn.setColor('#ffffff')` (pointerout) | `startBtn.setColor(THEME.ui.textPrimary.hex)` | `.hex` (Pattern C) |
| 63   | `color: '#445566'` (footer) | `color: THEME.ui.textMuted.hex` | `.hex` (Pattern B) |

Collapse rationale per D-06: `#668899` (subtitle) and `#445566` (footer) both semantically render as "dim supporting text" → both become `ui.textMuted`. Title green `#00ff88` and hover green `#00ff88` both become `ui.accentPrimary`. Default white `#ffffff` and hover-out white `#ffffff` both become `ui.textPrimary`.

**Step 3 — Guard rails (cite 06-RESEARCH.md Pitfalls):**

- Pitfall #1: Do NOT pass a hex string to `lineStyle()` — it takes a number. Line 23 uses `.num`.
- Pitfall #2: Do NOT pass `0x...` to text style `color:` — it takes a string. Lines 30, 37, 44, 63 use `.hex`.
- Pitfall #3: Do NOT pass the bare `THEME.ui.accentPrimary` object — always use `.hex` or `.num` suffix.

Out of scope in this file (leave untouched):
- Line 74 `g.fillStyle(0xffffff, alpha)` inside `createStarfield()` — procedural game-world art per D-01
- Any vignette/POSTFX config references (Phase 5 domain)
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | grep -q error &amp;&amp; exit 1; rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/scenes/MainMenuScene.js | rg -v '0xffffff.*createStarfield|g\.fillStyle\(0xffffff' | rg -v '^src/scenes/MainMenuScene\.js:74:' &amp;&amp; (echo "hex literals remain outside starfield carve-out"; exit 1) || echo "file clean"; rg -c 'THEME\.ui\.' src/scenes/MainMenuScene.js | grep -qE '^[7-9]$|^1[0-9]$' || (echo "expected >=7 THEME.ui.* refs"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/scenes/MainMenuScene.js` returns only the line-74 starfield `0xffffff` carve-out (one match, documented)
    - `rg 'THEME\.ui\.' src/scenes/MainMenuScene.js` returns at least 7 matches (one per migrated literal)
    - `rg "import.*THEME.*from '\.\./config/GameConfig\.js'" src/scenes/MainMenuScene.js` returns 1 match
    - Lines 23, 30, 37, 44, 47, 48, 63 no longer contain their pre-plan hardcoded literals
    - `npm run build` exits 0 with no warnings
    - No edits outside the 7 literal lines + import line
  </acceptance_criteria>
  <done>
MainMenuScene.js imports `THEME` and references `THEME.ui.*.hex` / `.num` at all 7 call sites. Line 74 starfield `0xffffff` intentionally retained (D-01 boundary). Build passes. Visual: main menu renders with cosmic purple accent instead of green; subtitle/footer in muted purple tone; gridline unchanged chromatically.
  </done>
</task>

</tasks>

<verification>
```bash
# File scan: only starfield line-74 should remain
rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/scenes/MainMenuScene.js

# Migration count
rg -c 'THEME\.ui\.' src/scenes/MainMenuScene.js   # ≥ 7

# Import check
rg 'import .*THEME.*GameConfig' src/scenes/MainMenuScene.js   # 1 match

# Build
npm run build   # exit 0
```
</verification>

<success_criteria>
- MainMenuScene.js consumes THEME.ui.* for grid/title/subtitle/start-button/footer
- Zero hardcoded UI-chrome color literals remain (starfield line-74 is explicit D-01 carve-out)
- 7 literal replacements complete (line 23 numeric, lines 30/37/44/47/48/63 string)
- Build passes with no warnings
- THEME-03 progress: 1 of 6 consumer files migrated
</success_criteria>

<output>
After completion, create `.planning/phases/06-cohesive-theme/06-02-SUMMARY.md` documenting:
- 7 literal replacements with before/after values
- Confirmation the starfield line-74 carve-out was left intact
- Build log outcome
</output>
