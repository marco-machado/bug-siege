---
phase: 06-cohesive-theme
plan: 05
plan_id: 06-05
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/scenes/GameOverScene.js
autonomous: true
requirements: [THEME-03]
tags: [theme, scene, refactor]

must_haves:
  truths:
    - "GameOverScene imports THEME from GameConfig.js"
    - "Win/lose title ternary uses accentPrimary (victory) / danger (defeat)"
    - "Stats text, restart button, menu button, and both pointer hover pairs all consume THEME.ui.*"
    - "npm run build exits 0 with no warnings"
  artifacts:
    - path: "src/scenes/GameOverScene.js"
      provides: "End screen consuming THEME.ui palette"
      contains: "THEME.ui.accentPrimary.hex"
  key_links:
    - from: "src/scenes/GameOverScene.js"
      to: "src/config/GameConfig.js"
      via: "import { GAME, POSTFX, THEME } from '../config/GameConfig.js'"
      pattern: "import.*THEME.*from.*GameConfig"
---

<objective>
Migrate all 8 hardcoded UI-chrome color literal occurrences in `src/scenes/GameOverScene.js` (victory/defeat title ternary, stats text, restart button + hover pair, menu button + hover pair) to `THEME.ui.*` constants. The hover structure is identical to MainMenuScene (Pattern C) — literal-only swap, no structural changes.

Purpose: Unify the end-screen chrome with the cosmic palette. Victory title flips from green `#00ff88` to cosmic purple `ui.accentPrimary`; defeat retains red but via the semantic `ui.danger`; button states (default white → `textPrimary`, hover → `accentPrimary`) match the main menu's interaction vocabulary.

Output: GameOverScene.js renders victory/defeat screen from THEME.ui.*; build passes; file contains zero hardcoded UI color literals.
</objective>

<execution_context>
@/Users/machado/Projects/bug-siege/.claude/get-shit-done/workflows/execute-plan.md
@/Users/machado/Projects/bug-siege/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-cohesive-theme/06-CONTEXT.md
@.planning/phases/06-cohesive-theme/06-PATTERNS.md
@.planning/phases/06-cohesive-theme/06-01-SUMMARY.md
@src/scenes/GameOverScene.js
@src/config/GameConfig.js

<interfaces>
<!-- Existing import (GameOverScene.js:2):
  import { GAME, POSTFX } from '../config/GameConfig.js';
  → becomes:
  import { GAME, POSTFX, THEME } from '../config/GameConfig.js';
-->

<!-- API-to-form rule: every literal in this file is a text style `color:` property or a `setColor()` call.
  All replacements are `.hex` form.
-->

<!-- Pattern I (ternary text color) for line 26 and Pattern C (hover pair) for lines 57-58, 67-68:
  See 06-PATTERNS.md lines 220-244 (Pattern C) and lines 411-440 (Pattern I).
-->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migrate GameOverScene.js (8 literal occurrences) to THEME.ui.*</name>
  <files>src/scenes/GameOverScene.js</files>
  <read_first>
    - src/scenes/GameOverScene.js (full file — ~75 lines)
    - .planning/phases/06-cohesive-theme/06-PATTERNS.md section "`src/scenes/GameOverScene.js`" lines 71-84
    - .planning/phases/06-cohesive-theme/06-PATTERNS.md "Pattern C" (hover pair), "Pattern I" (ternary text color)
    - .planning/phases/06-cohesive-theme/06-CONTEXT.md (D-06 collapse `#00ff88` → accentPrimary, split `danger` for destructive semantic)
  </read_first>
  <action>
**Step 1 — Extend the import (line 2):**

Before:
```javascript
import { GAME, POSTFX } from '../config/GameConfig.js';
```

After:
```javascript
import { GAME, POSTFX, THEME } from '../config/GameConfig.js';
```

**Step 2 — Replace the 8 literal occurrences** (per 06-PATTERNS.md Call-Site Inventory for GameOverScene):

| Line | Before | After | Form |
|------|--------|-------|------|
| 26 | `const color = won ? '#00ff88' : '#ff3333';` | `const color = won ? THEME.ui.accentPrimary.hex : THEME.ui.danger.hex;` | `.hex` (both branches) |
| 46 | `color: '#ffffff'` (stats text) | `color: THEME.ui.textPrimary.hex` | `.hex` |
| 54 | `color: '#ffffff'` (restart button default) | `color: THEME.ui.textPrimary.hex` | `.hex` |
| 57 | `restartBtn.setColor('#00ff88')` (pointerover) | `restartBtn.setColor(THEME.ui.accentPrimary.hex)` | `.hex` |
| 58 | `restartBtn.setColor('#ffffff')` (pointerout) | `restartBtn.setColor(THEME.ui.textPrimary.hex)` | `.hex` |
| 64 | `color: '#ffffff'` (menu button default) | `color: THEME.ui.textPrimary.hex` | `.hex` |
| 67 | `menuBtn.setColor('#00ff88')` (pointerover) | `menuBtn.setColor(THEME.ui.accentPrimary.hex)` | `.hex` |
| 68 | `menuBtn.setColor('#ffffff')` (pointerout) | `menuBtn.setColor(THEME.ui.textPrimary.hex)` | `.hex` |

Line 26 is one source line but counts as two distinct literal replacements (both branches of the ternary). Total replacement count across all 7 source lines: 8.

**Collapse rationale (D-06):**
- All `#ffffff` → `ui.textPrimary` (intent: "default UI text")
- All `#00ff88` → `ui.accentPrimary` (intent: "active/positive accent" — victory, button hover)
- `#ff3333` → `ui.danger` (intent: "destructive/defeat" — explicit gameplay-state split from accentPrimary)

Visual result: Victory shows cosmic purple title; defeat retains red (but now via semantic key); both restart/menu buttons transition from light accent `#eef2ff` (textPrimary) on hover-out to cosmic purple `#9966ff` (accentPrimary) on hover-in, matching MainMenuScene.

**Guard rails (06-RESEARCH.md Pitfalls):**
- All 8 migrations are `.hex` form — text styles and `setColor()` exclusively. Do NOT use `.num`.
- Do NOT pass the bare `THEME.ui.accentPrimary` object — always `.hex`.
- Do NOT touch any vignette / POSTFX call (Phase 5 domain, already owns its own config).

No out-of-scope carve-outs in this file — every literal migrates.
  </action>
  <verify>
    <automated>npm run build 2>&amp;1 | grep -q error &amp;&amp; exit 1; rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/scenes/GameOverScene.js &amp;&amp; (echo "hex literals remain"; exit 1) || echo "file clean"; rg -c 'THEME\.ui\.' src/scenes/GameOverScene.js | grep -qE '^([8-9]|1[0-9])$' || (echo "expected ≥8 THEME.ui.* refs"; exit 1); rg -c 'THEME\.ui\.accentPrimary\.hex' src/scenes/GameOverScene.js | grep -qE '^[3-9]$' || (echo "expected ≥3 accentPrimary refs (victory title + 2 hover-overs)"; exit 1)</automated>
  </verify>
  <acceptance_criteria>
    - `rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/scenes/GameOverScene.js` returns zero matches
    - `rg 'THEME\.ui\.' src/scenes/GameOverScene.js` returns at least 8 matches
    - `rg 'THEME\.ui\.accentPrimary\.hex' src/scenes/GameOverScene.js` returns at least 3 matches (line 26 victory branch + line 57 restart hover + line 67 menu hover)
    - `rg 'THEME\.ui\.danger\.hex' src/scenes/GameOverScene.js` returns exactly 1 match (line 26 defeat branch)
    - `rg 'THEME\.ui\.textPrimary\.hex' src/scenes/GameOverScene.js` returns at least 4 matches (lines 46, 54, 58, 64, 68 — 5 occurrences across 5 lines)
    - `rg "import.*THEME.*from '\.\./config/GameConfig\.js'" src/scenes/GameOverScene.js` returns 1 match
    - `npm run build` exits 0 with no warnings
  </acceptance_criteria>
  <done>
GameOverScene.js renders victory title in cosmic purple, defeat title in red, all buttons with textPrimary-default / accentPrimary-hover states, stats in textPrimary. Zero hardcoded color literals remain. Build passes.
  </done>
</task>

</tasks>

<verification>
```bash
# File should be 100% clean
rg '(#[0-9a-fA-F]{6}|0x[0-9a-fA-F]{6})' src/scenes/GameOverScene.js
# → zero

# Reference count
rg 'THEME\.ui\.' src/scenes/GameOverScene.js | wc -l   # ≥ 8

# Specific semantic balance
rg 'THEME\.ui\.accentPrimary\.hex' src/scenes/GameOverScene.js   # ≥ 3
rg 'THEME\.ui\.danger\.hex' src/scenes/GameOverScene.js          # = 1
rg 'THEME\.ui\.textPrimary\.hex' src/scenes/GameOverScene.js     # ≥ 4

# Build
npm run build   # exit 0
```
</verification>

<success_criteria>
- GameOverScene.js consumes THEME.ui.* for victory/defeat title, stats, both buttons, all hover states
- Zero hardcoded color literals remain
- 8 literal occurrences replaced (line 26 counts as two via ternary)
- Build passes with no warnings
- THEME-03 progress: 4 of 6 consumer files migrated
</success_criteria>

<output>
After completion, create `.planning/phases/06-cohesive-theme/06-05-SUMMARY.md` documenting:
- 8 literal replacements with before/after values
- Call-out: line 26 ternary → two semantic keys
- Build log outcome
</output>
